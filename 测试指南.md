# 完整主流程测试指南 - 教师和学生双角色视角

本指南将从**教师**和**学生**两个角色的视角，完整测试系统的所有主流程接口。

## 📋 测试环境准备

### 1. 启动后端服务
```powershell
cd Face-Recognition/back
node app.js
```

**预期输出：**
```
服务器已启动，HTTP 端口: 3000
Socket.IO 已启用，WebSocket 端口: 3000
```

### 2. 安装 Socket.IO 测试依赖（可选，用于命令行测试）
```powershell
cd Face-Recognition/back
npm install socket.io-client
```

### 3. 确认数据库配置
- 数据库：MySQL，库名：`face_recognition`
- 确保已执行 `database_schema.sql` 初始化表结构
- 确保已预置班级数据和教师账号（见下文）

### 4. 准备测试图片
- 至少准备2-3张清晰的人脸照片（不同角度）
- 建议路径：`back/public/` 目录下
- 格式：`.jpg`, `.jpeg`, `.png`
- 大小：< 20MB

### 5. 预置数据检查
**预置教师账号：**
- teacher001 (张老师) - 密码: 123456
- teacher002 (李老师) - 密码: 123456
- teacher003 (王老师) - 密码: 123456
- teacher004 (赵老师) - 密码: 123456
- teacher005 (刘老师) - 密码: 123456

**预置班级（示例）：**
- 计算机科学与技术2023级1班 (classId: 1)
- 计算机科学与技术2023级2班 (classId: 2)
- 软件工程2023级1班 (classId: 3)

---

## 🎓 学生角色完整流程测试

### 阶段一：学生注册与登录

#### 步骤1.1：获取班级列表（注册前）
**接口：** `GET /api/classes`

**PowerShell:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/classes" -Method GET -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe:**
```bat
curl.exe -s "http://localhost:3000/api/classes"
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取班级列表成功",
    "data": [
        {
            "id": 1,
            "className": "计算机科学与技术2023级1班",
            "classCode": "CS2023-1"
        },
        ...
    ]
}
```

**记录：** 选择一个班级ID（如：`classId = 1`），用于后续注册。

---

#### 步骤1.2：学生注册
**接口：** `POST /api/register`

**PowerShell:**
```powershell
# 生成唯一账号
$randomId = Get-Random -Minimum 1000 -Maximum 9999
$account = "student$randomId"

$body = @{
    userAccount = $account
    userPassword = "Test123"
    userName = "测试学生"
    classId = 1  # 使用步骤1.1获取的班级ID
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:3000/api/register" -Method POST -Body $body -ContentType "application/json" -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe:**
```bat
curl.exe -s -X POST "http://localhost:3000/api/register" ^
  -H "Content-Type: application/json" ^
  -d "{\"userAccount\":\"student001\",\"userPassword\":\"Test123\",\"userName\":\"测试学生\",\"classId\":1}"
```

**预期响应：**
```json
{
    "success": true,
    "message": "注册成功",
    "data": {
        "userId": 10,
        "userAccount": "student001",
        "userName": "测试学生",
        "userRole": "student",
        "classId": 1,
        "className": "计算机科学与技术2023级1班"
    }
}
```

**记录：** 保存 `userId` 和 `userAccount`（如：`userId = 10, userAccount = student001`）

---

#### 步骤1.3：学生登录
**接口：** `POST /api/login`

**PowerShell（保存Session）:**
```powershell
$loginBody = @{
    userAccount = "student001"
    userPassword = "Test123"
} | ConvertTo-Json

$loginResponse = Invoke-WebRequest -Uri "http://localhost:3000/api/login" -Method POST -Body $loginBody -ContentType "application/json" -UseBasicParsing -SessionVariable studentSession
$loginResponse.Content

# 验证登录结果，提取userId和faceRegistered状态
$loginData = $loginResponse.Content | ConvertFrom-Json
$studentUserId = $loginData.data.userId
$faceRegistered = $loginData.data.faceRegistered
Write-Host "学生ID: $studentUserId, 人脸已注册: $faceRegistered"
```

**curl.exe（保存Cookie）:**
```bat
curl.exe -s -X POST "http://localhost:3000/api/login" ^
  -H "Content-Type: application/json" ^
  -d "{\"userAccount\":\"student001\",\"userPassword\":\"Test123\"}" ^
  --cookie-jar student_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "登录成功",
    "data": {
        "userId": 10,
        "userAccount": "student001",
        "userName": "测试学生",
        "userRole": "student",
        "classId": 1,
        "className": "计算机科学与技术2023级1班",
        "faceRegistered": 0
    }
}
```

---

#### 步骤1.4：获取当前用户信息
**接口：** `GET /api/user-info`

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/user-info" -Method GET -UseBasicParsing -WebSession $studentSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s "http://localhost:3000/api/user-info" --cookie student_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取用户信息成功",
    "data": {
        "userId": 10,
        "userAccount": "student001",
        "userName": "测试学生",
        "userRole": "student",
        "classId": 1,
        "className": "计算机科学与技术2023级1班",
        "faceRegistered": 0
    }
}
```

---

### 阶段二：学生人脸注册

#### 步骤2.1：首次人脸注册（冷启动）
**接口：** `POST /api/face-register`

**PowerShell:**
```powershell
# 替换为实际的图片路径
$imagePath = "F:\code\faceRecognition\Face-Recognition\back\public\test_student_face1.jpg"

$form = @{
    imagefile = Get-Item $imagePath
    userId = $studentUserId  # 使用步骤1.3记录的userId
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-register" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe（CMD - 多种方式）：**
```bat
REM 方式1：使用双反斜杠转义（推荐，解决"拒绝访问"问题）
curl.exe -s -X POST "http://localhost:3000/api/face-register" ^
  -F "userId=6" ^
  -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\imagefile-1761749440976-903074187.jpg;type=image/jpeg"

REM 方式2：使用相对路径（当前目录在 back 下时使用）
curl.exe -s -X POST "http://localhost:3000/api/face-register" ^
  -F "userId=6" ^
  -F "imagefile=@public\imagefile-1761749440976-903074187.jpg;type=image/jpeg"

REM 方式3：单行命令（避免续行问题）
curl.exe -s -X POST "http://localhost:3000/api/face-register" -F "userId=6" -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\imagefile-1761749440976-903074187.jpg;type=image/jpeg"
```

**预期响应（首次注册）：**
```json
{
    "success": true,
    "message": "人脸注册成功（首次注册）",
    "data": {
        "userId": 10,
        "savedToDataset": true,
        "retrained": true,
        "coldStart": true
    }
}
```

**验证点：**
- ✅ `coldStart: true` 表示首次注册，跳过了识别校验
- ✅ `savedToDataset: true` 表示图片已保存到训练集
- ✅ `retrained: true` 表示模型已重新训练
- ✅ 检查 `opencv/face_get/Facedata/` 目录是否有新文件
- ✅ 检查用户表的 `faceRegistered` 字段是否变为 `1`

---

#### 步骤2.2：追加人脸样本（非首次注册）
**接口：** `POST /api/face-register`

**说明：** 使用同一学生的不同照片进行追加注册，这次会先进行识别校验。

**PowerShell:**
```powershell
# 使用同一学生的另一张照片
$imagePath2 = "F:\code\faceRecognition\Face-Recognition\back\public\test_student_face2.jpg"

$form = @{
    imagefile = Get-Item $imagePath2
    userId = $studentUserId
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-register" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe（CMD - 多种方式）：**
```bat
REM 方式1：使用双反斜杠转义（推荐，解决"拒绝访问"问题）
curl.exe -s -X POST "http://localhost:3000/api/face-register" ^
  -F "userId=6" ^
  -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\imagefile-1761749440976-903074187.jpg;type=image/jpeg"

REM 方式2：使用相对路径（当前目录在 back 下时使用）
curl.exe -s -X POST "http://localhost:3000/api/face-register" ^
  -F "userId=6" ^
  -F "imagefile=@public\imagefile-1761749440976-903074187.jpg;type=image/jpeg"

REM 方式3：单行命令（避免续行问题）
curl.exe -s -X POST "http://localhost:3000/api/face-register" -F "userId=6" -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\imagefile-1761749440976-903074187.jpg;type=image/jpeg"
```

**预期响应（非首次注册）：**
```json
{
    "success": true,
    "message": "人脸注册成功",
    "data": {
        "userId": 10,
        "savedToDataset": true,
        "retrained": true,
        "coldStart": false
    }
}
```

**验证点：**
- ✅ `coldStart: false` 表示非首次注册，已进行识别校验
- ✅ 只有识别出是该学生的人脸才会成功
- ✅ 如果上传他人照片，应返回 `"人脸注册失败：未识别到该用户的人脸"`

---

### 阶段三：查看签到任务（支持 Socket.IO 实时推送）

#### 步骤3.1：查看学生班级的签到任务列表
**接口：** `GET /api/attendance-tasks`

**说明：** 可以通过 REST API 或 Socket.IO 获取任务列表。推荐使用 Socket.IO 实时推送接收新任务。

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-tasks" -Method GET -UseBasicParsing -WebSession $studentSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s "http://localhost:3000/api/attendance-tasks" --cookie student_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取签到任务列表成功",
    "data": [
        {
            "id": 1,
            "taskName": "早课签到",
            "teacherId": 5,
            "classId": 1,
            "startTime": "2024-01-15 08:00:00",
            "endTime": "2024-01-15 09:00:00",
            "status": "active",
            "createTime": "2024-01-14 10:00:00",
            "className": "计算机科学与技术2023级1班",
            "teacherName": "张老师"
        },
        ...
    ]
}
```

**记录：** 如果有活动任务，保存 `taskId`（如：`taskId = 1`）

**验证点：**
- ✅ 只能看到自己班级（classId匹配）的任务
- ✅ 看不到其他班级的任务

---

#### 步骤3.2：使用 Socket.IO 连接并监听实时推送（推荐）
**工具：** 使用命令行测试脚本或前端连接 Socket.IO

**方式一：使用命令行测试脚本（最简单）**

```bash
cd Face-Recognition/back
npm install socket.io-client  # 仅第一次需要
node test_socket_cli.js
```

测试脚本会自动完成：
1. ✅ 学生登录获取 Session
2. ✅ 连接 Socket.IO
3. ✅ 自动加入班级房间
4. ✅ 请求任务列表
5. ✅ 老师发布任务（自动触发）
6. ✅ 接收实时推送消息

**预期输出：**
```
✅ [时间] Socket 连接成功
✅ [时间] 身份验证成功
📝 [时间] 加入房间: class-1
✅ [时间] 任务发布成功
🎉 [时间] 收到新任务推送！
  任务名称: 命令行测试任务
  任务ID: 1
  ...
```

**方式二：使用 REST API 测试（备用）**

如果 Socket.IO 不可用，仍可使用 REST API 轮询方式获取任务列表（见步骤3.1）。

**验证点：**
- ✅ Socket.IO 连接成功
- ✅ 身份验证成功，自动加入班级房间
- ✅ 老师发布任务后，学生立即收到推送
- ✅ 推送的任务信息完整（包含所有字段）

---

### 阶段四：学生人脸识别签到

#### 步骤4.1：人脸识别（不带签到任务）
**接口：** `POST /api/face-recognition`

**PowerShell:**
```powershell
$imagePath = "F:\code\faceRecognition\Face-Recognition\back\public\test_student_face1.jpg"

$form = @{
    imagefile = Get-Item $imagePath
    # 不传taskId，只进行识别
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-recognition" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe（使用系统路径）：**
```bat
C:\Windows\System32\curl.exe -s -X POST "http://localhost:3000/api/face-recognition" ^
  -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\test_student_face1.jpg;type=image/jpeg"
```

**预期响应：**
```json
{
    "success": true,
    "message": "人脸识别成功，已自动签到",
    "data": {
        "recognized": true,
        "userId": 10,
        "userAccount": "student001",
        "userName": "测试学生",
        "attendanceRecorded": false,
        "taskId": null
    }
}
```

**验证点：**
- ✅ `recognized: true` 表示识别成功
- ✅ `attendanceRecorded: false` 表示未关联签到任务（未传taskId）
- ✅ 识别出的用户信息正确

---

#### 步骤4.2：人脸识别签到（带任务ID）
**接口：** `POST /api/face-recognition`

**说明：** 使用已注册的学生照片，并传入有效的签到任务ID进行签到。

**PowerShell:**
```powershell
$imagePath = "F:\code\faceRecognition\Face-Recognition\back\public\test_student_face1.jpg"
$taskId = 1  # 使用步骤3.1获取的taskId

$form = @{
    imagefile = Get-Item $imagePath
    taskId = $taskId
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-recognition" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**curl.exe（使用系统路径）：**
```bat
C:\Windows\System32\curl.exe -s -X POST "http://localhost:3000/api/face-recognition" ^
  -F "taskId=1" ^
  -F "imagefile=@F:\\code\\faceRecognition\\Face-Recognition\\back\\public\\test_student_face1.jpg;type=image/jpeg"
```

**预期响应：**
```json
{
    "success": true,
    "message": "人脸识别成功，已自动签到",
    "data": {
        "recognized": true,
        "userId": 10,
        "userAccount": "student001",
        "userName": "测试学生",
        "attendanceRecorded": true,
        "taskId": 1
    }
}
```

**验证点：**
- ✅ `recognized: true` 表示识别成功
- ✅ `attendanceRecorded: true` 表示已记录签到
- ✅ `taskId` 正确关联到签到任务
- ✅ 检查数据库 `attendance_record` 表是否有新记录

---

#### 步骤4.3：测试识别失败场景
**接口：** `POST /api/face-recognition`

**说明：** 使用未注册用户的照片进行识别。

**PowerShell:**
```powershell
# 使用未注册用户的照片
$unknownImagePath = "F:\code\faceRecognition\Face-Recognition\back\public\unknown_face.jpg"

$form = @{
    imagefile = Get-Item $unknownImagePath
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-recognition" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**预期响应：**
```json
{
    "success": true,
    "message": "未识别到已知人脸",
    "data": {
        "recognized": false,
        "attendanceRecorded": false
    }
}
```

**验证点：**
- ✅ `recognized: false` 表示未识别到已知人脸
- ✅ 系统应该正确处理识别失败的情况

---

### 阶段五：学生登出

#### 步骤5.1：学生登出
**接口：** `POST /api/logout`

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/logout" -Method POST -UseBasicParsing -WebSession $studentSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s -X POST "http://localhost:3000/api/logout" --cookie student_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "登出成功"
}
```

**验证点：**
- ✅ 登出后再次访问 `/api/user-info` 应返回 `"未登录"`

---

## 👨‍🏫 教师角色完整流程测试

### 阶段一：教师登录

#### 步骤1.1：教师登录
**接口：** `POST /api/login`

**PowerShell（保存Session）:**
```powershell
$teacherLoginBody = @{
    userAccount = "teacher001"
    userPassword = "123456"
} | ConvertTo-Json

$teacherLoginResponse = Invoke-WebRequest -Uri "http://localhost:3000/api/login" -Method POST -Body $teacherLoginBody -ContentType "application/json" -UseBasicParsing -SessionVariable teacherSession
$teacherLoginResponse.Content

# 提取教师信息
$teacherData = $teacherLoginResponse.Content | ConvertFrom-Json
$teacherUserId = $teacherData.data.userId
$teacherClassId = $teacherData.data.classId
Write-Host "教师ID: $teacherUserId, 班级ID: $teacherClassId"
```

**curl.exe（保存Cookie）:**
```bat
curl.exe -s -X POST "http://localhost:3000/api/login" ^
  -H "Content-Type: application/json" ^
  -d "{\"userAccount\":\"teacher001\",\"userPassword\":\"123456\"}" ^
  --cookie-jar teacher_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "登录成功",
    "data": {
        "userId": 5,
        "userAccount": "teacher001",
        "userName": "张老师",
        "userRole": "teacher",
        "classId": 1,
        "className": "计算机科学与技术2023级1班",
        "faceRegistered": 0
    }
}
```

**记录：** 保存 `userId` 和 `classId`（如：`userId = 5, classId = 1`）

---

#### 步骤1.2：获取教师信息
**接口：** `GET /api/user-info`

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/user-info" -Method GET -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s "http://localhost:3000/api/user-info" --cookie teacher_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取用户信息成功",
    "data": {
        "userId": 5,
        "userAccount": "teacher001",
        "userName": "张老师",
        "userRole": "teacher",
        "classId": 1,
        "className": "计算机科学与技术2023级1班",
        "faceRegistered": 0
    }
}
```

---

### 阶段二：教师发布签到任务（含实时推送）

#### 步骤2.1：发布签到任务
**接口：** `POST /api/attendance-task`

**说明：** 教师只能为自己所在的班级发布签到任务。**任务发布成功后，系统会自动通过 Socket.IO 实时推送给对应班级的所有在线学生。**

**PowerShell（使用Session）:**
```powershell
# 设置任务时间（结束时间必须晚于当前时间）
$startTime = (Get-Date).AddMinutes(5).ToString("yyyy-MM-dd HH:mm:ss")
$endTime = (Get-Date).AddHours(2).ToString("yyyy-MM-dd HH:mm:ss")

$taskBody = @{
    taskName = "早课签到测试"
    classId = $teacherClassId  # 必须是教师所在的班级
    startTime = $startTime
    endTime = $endTime
} | ConvertTo-Json

$taskResponse = Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-task" -Method POST -Body $taskBody -ContentType "application/json" -UseBasicParsing -WebSession $teacherSession
$taskResponse.Content

# 提取任务ID
$taskData = $taskResponse.Content | ConvertFrom-Json
$taskId = $taskData.data.taskId
Write-Host "签到任务ID: $taskId"
```

**curl.exe（使用Cookie）:**
```bat
REM 注意：需要替换为实际的时间
curl.exe -s -X POST "http://localhost:3000/api/attendance-task" ^
  -H "Content-Type: application/json" ^
  --cookie teacher_cookies.txt ^
  -d "{\"taskName\":\"早课签到测试\",\"classId\":1,\"startTime\":\"2024-01-15 08:00:00\",\"endTime\":\"2024-01-15 10:00:00\"}"
```

**预期响应：**
```json
{
    "success": true,
    "message": "签到任务发布成功",
    "data": {
        "taskId": 1,
        "taskName": "早课签到测试",
        "classId": 1,
        "startTime": "2024-01-15 08:00:00",
        "endTime": "2024-01-15 10:00:00"
    }
}
```

**记录：** 保存 `taskId`（如：`taskId = 1`）

**验证点：**
- ✅ 只能为自己所在的班级发布任务
- ✅ 如果尝试为其他班级发布任务，应返回 `"您只能为自己所在的班级发布签到任务"`
- ✅ 结束时间必须晚于当前时间
- ✅ 开始时间必须早于结束时间
- ✅ **任务发布成功后，服务器日志应显示：`[Socket.IO] 签到任务已推送给房间: class-X, 任务ID: X`**
- ✅ **如果有在线学生连接了 Socket.IO，学生应立即收到 `new-task` 推送事件**

---

#### 步骤2.2：测试实时推送功能
**前提条件：** 需要有两个客户端：
1. **学生端**：已连接 Socket.IO（可以使用 `test_socket_cli.js` 脚本）
2. **教师端**：发布签到任务

**测试步骤：**

**步骤 A：启动学生 Socket 连接**
```bash
# 终端1：运行测试脚本（学生端）
cd Face-Recognition/back
node test_socket_cli.js
```

等待脚本输出：
```
✅ Socket 连接成功
✅ 身份验证成功
📝 加入房间: class-1
```

**步骤 B：老师发布任务（触发推送）**
```bash
# 终端2：使用 curl 发布任务
curl.exe -s -X POST "http://localhost:3000/api/attendance-task" ^
  -H "Content-Type: application/json" ^
  --cookie teacher_cookies.txt ^
  -d "{\"taskName\":\"实时推送测试任务\",\"classId\":1,\"startTime\":\"2024-12-25 20:00:00\",\"endTime\":\"2024-12-25 22:00:00\"}"
```

**预期结果：**
- ✅ 终端2（老师端）：返回任务发布成功
- ✅ 终端1（学生端）：**立即显示** `🎉 收到新任务推送！`
- ✅ 服务器日志：`[Socket.IO] 签到任务已推送给房间: class-1, 任务ID: X`

**验证点：**
- ✅ 学生无需轮询 API，即可实时收到新任务
- ✅ 推送延迟极低（几乎实时）
- ✅ 只有目标班级的学生收到推送

---

#### 步骤2.3：测试权限验证（尝试跨班级发布任务）
**接口：** `POST /api/attendance-task`

**说明：** 测试教师是否能跨班级发布任务（应该失败）。

**PowerShell:**
```powershell
# 尝试为其他班级发布任务（假设教师所在班级是1，尝试为班级2发布）
$invalidTaskBody = @{
    taskName = "非法任务"
    classId = 2  # 不是教师所在的班级
    startTime = (Get-Date).AddMinutes(5).ToString("yyyy-MM-dd HH:mm:ss")
    endTime = (Get-Date).AddHours(2).ToString("yyyy-MM-dd HH:mm:ss")
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-task" -Method POST -Body $invalidTaskBody -ContentType "application/json" -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**预期响应：**
```json
{
    "success": false,
    "message": "您只能为自己所在的班级发布签到任务"
}
```

**验证点：**
- ✅ 权限验证正确，阻止了跨班级发布任务

---

### 阶段三：教师查看签到任务列表（含 Socket.IO 推送验证）

#### 步骤3.1：查看教师发布的签到任务
**接口：** `GET /api/attendance-tasks`

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-tasks" -Method GET -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s "http://localhost:3000/api/attendance-tasks" --cookie teacher_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取签到任务列表成功",
    "data": [
        {
            "id": 1,
            "taskName": "早课签到测试",
            "teacherId": 5,
            "classId": 1,
            "startTime": "2024-01-15 08:00:00",
            "endTime": "2024-01-15 10:00:00",
            "status": "active",
            "createTime": "2024-01-14 10:00:00",
            "className": "计算机科学与技术2023级1班",
            "teacherName": "张老师"
        }
    ]
}
```

**验证点：**
- ✅ 只能看到自己发布的任务
- ✅ 看不到其他教师发布的任务
- ✅ 如果发布任务时已有学生连接 Socket.IO，学生应实时收到推送

---

#### 步骤3.2：按状态筛选任务
**接口：** `GET /api/attendance-tasks?status=active`

**PowerShell:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-tasks?status=active" -Method GET -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**预期响应：** 只返回状态为 `active` 的任务

---

### 阶段四：教师查看签到统计

#### 步骤4.1：查看签到统计
**接口：** `GET /api/attendance-stats?taskId=xxx`

**说明：** 只有教师可以查看签到统计，需要传入有效的taskId。

**PowerShell（使用Session）:**
```powershell
$taskId = 1  # 使用步骤2.1获取的taskId

Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-stats?taskId=$taskId" -Method GET -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**curl.exe（使用Cookie）:**
```bat
curl.exe -s "http://localhost:3000/api/attendance-stats?taskId=1" --cookie teacher_cookies.txt
```

**预期响应：**
```json
{
    "success": true,
    "message": "获取签到统计成功",
    "data": {
        "task": {
            "id": 1,
            "taskName": "早课签到测试",
            "teacherId": 5,
            "classId": 1,
            "startTime": "2024-01-15 08:00:00",
            "endTime": "2024-01-15 10:00:00",
            "status": "active"
        },
        "totalStudents": 10,
        "checkedStudents": 1,
        "attendanceRate": "10.00%",
        "details": [
            {
                "userName": "测试学生",
                "userAccount": "student001",
                "checkTime": "2024-01-15 08:15:30",
                "status": 1
            }
        ]
    }
}
```

**验证点：**
- ✅ `totalStudents` 表示班级总学生数
- ✅ `checkedStudents` 表示已签到学生数
- ✅ `attendanceRate` 表示签到率
- ✅ `details` 包含详细的签到记录
- ✅ 如果学生已经完成签到（从阶段四步骤4.2），应该能看到记录

---

#### 步骤4.2：测试学生角色访问签到统计（应该失败）
**接口：** `GET /api/attendance-stats?taskId=xxx`

**说明：** 使用学生Session尝试访问签到统计接口（应该返回403）。

**PowerShell:**
```powershell
# 先用学生账号登录
$studentLogin = @{
    userAccount = "student001"
    userPassword = "Test123"
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://localhost:3000/api/login" -Method POST -Body $studentLogin -ContentType "application/json" -UseBasicParsing -SessionVariable studentSession2 | Out-Null

# 尝试访问签到统计（应该失败）
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-stats?taskId=1" -Method GET -UseBasicParsing -WebSession $studentSession2 | Select-Object -ExpandProperty Content
```

**预期响应：**
```json
{
    "success": false,
    "message": "只有老师可以查看签到统计"
}
```

**验证点：**
- ✅ 权限验证正确，学生无法访问教师专用接口

---

### 阶段五：教师人脸注册（可选）

#### 步骤5.1：教师人脸注册（首次）
**接口：** `POST /api/face-register`

**说明：** 教师也可以注册人脸（虽然不用于签到，但可以用于身份验证等）。

**PowerShell:**
```powershell
$teacherFaceImage = "F:\code\faceRecognition\Face-Recognition\back\public\test_teacher_face.jpg"

$form = @{
    imagefile = Get-Item $teacherFaceImage
    userId = $teacherUserId  # 教师ID
}

Invoke-WebRequest -Uri "http://localhost:3000/api/face-register" -Method POST -Form $form -UseBasicParsing | Select-Object -ExpandProperty Content
```

**预期响应：** 同学生人脸注册

---

### 阶段六：教师登出

#### 步骤6.1：教师登出
**接口：** `POST /api/logout`

**PowerShell（使用Session）:**
```powershell
Invoke-WebRequest -Uri "http://localhost:3000/api/logout" -Method POST -UseBasicParsing -WebSession $teacherSession | Select-Object -ExpandProperty Content
```

**预期响应：**
```json
{
    "success": true,
    "message": "登出成功"
}
```

---

## 🔄 完整流程串联测试

### 场景一：完整的签到流程（含实时推送）

1. **教师端：**
   - 教师登录 → 发布签到任务 → 查看任务列表

2. **学生端：**
   - 学生注册 → 学生登录 → **连接 Socket.IO** → 人脸注册（首次）→ 查看签到任务 → **实时接收新任务推送** → 人脸识别签到（带taskId）

3. **教师端：**
   - 查看签到统计 → 验证学生签到记录

**预期结果：**
- ✅ 教师能看到签到任务和统计
- ✅ 学生能完成签到并看到任务
- ✅ 签到记录正确关联到任务
- ✅ **学生通过 Socket.IO 实时收到新任务推送，无需轮询**

### 场景二：Socket.IO 实时推送测试

**测试步骤：**

1. **学生连接 Socket.IO**
   ```bash
   # 终端1：运行测试脚本
   node test_socket_cli.js
   ```
   等待显示：`✅ 身份验证成功` 和 `📝 加入房间: class-1`

2. **老师发布任务**
   ```bash
   # 终端2：发布任务
   curl.exe -s -X POST "http://localhost:3000/api/attendance-task" \
     -H "Content-Type: application/json" \
     --cookie teacher_cookies.txt \
     -d "{\"taskName\":\"实时推送测试\",\"classId\":1,\"startTime\":\"2024-12-25 20:00:00\",\"endTime\":\"2024-12-25 22:00:00\"}"
   ```

3. **验证推送**
   - ✅ 终端1 应立即显示：`🎉 收到新任务推送！`
   - ✅ 服务器日志应显示：`[Socket.IO] 签到任务已推送给房间: class-1`

**预期结果：**
- ✅ 学生实时收到新任务，无需刷新或轮询
- ✅ 推送延迟极低（通常在 100ms 以内）
- ✅ 只有目标班级的在线学生收到推送

---

## 📊 测试结果验证清单

### 学生角色测试清单

- [ ] 1. 获取班级列表成功
- [ ] 2. 学生注册成功，返回userId和班级信息
- [ ] 3. 学生登录成功，Session正确创建
- [ ] 4. 获取用户信息成功，role为student
- [ ] 5. Socket.IO 连接成功（可选但推荐）
- [ ] 6. Socket.IO 身份验证成功，自动加入班级房间
- [ ] 7. 首次人脸注册成功（coldStart: true）
- [ ] 8. 追加人脸样本成功（coldStart: false）
- [ ] 9. 查看签到任务列表，只能看到自己班级的任务（REST API 或 Socket.IO）
- [ ] 10. **实时接收新任务推送（Socket.IO）**
- [ ] 11. 人脸识别成功（不带taskId）
- [ ] 12. 人脸识别签到成功（带taskId），attendanceRecorded: true
- [ ] 13. 识别失败场景正确处理
- [ ] 14. 学生登出成功

### 教师角色测试清单

- [ ] 1. 教师登录成功，role为teacher
- [ ] 2. 获取教师信息成功
- [ ] 3. 发布签到任务成功，taskId正确返回
- [ ] 4. **发布任务后，Socket.IO 实时推送给目标班级（服务器日志验证）**
- [ ] 5. 跨班级发布任务失败（权限验证正确）
- [ ] 6. 查看签到任务列表，只能看到自己发布的任务
- [ ] 7. 查看签到统计成功，统计数据正确
- [ ] 8. 学生角色无法访问签到统计（权限验证正确）
- [ ] 9. 教师人脸注册成功（可选）
- [ ] 10. 教师登出成功

### 集成测试清单

- [ ] 1. 教师发布任务后，学生能看到该任务（REST API 查询）
- [ ] 2. **教师发布任务后，在线学生通过 Socket.IO 实时收到推送**
- [ ] 3. **Socket.IO 推送的任务信息完整且正确**
- [ ] 4. 学生签到后，教师能看到签到记录
- [ ] 5. 签到统计数据正确（总人数、已签到、签到率）
- [ ] 6. 数据库记录正确（user、attendance_task、attendance_record表）

---

## ⚠️ 常见问题排查

### 1. 人脸识别失败
- **检查：** Python环境是否正确
- **检查：** `opencv/face_get/rec.py` 是否存在
- **检查：** `face_trainer/trainer.yml` 是否已生成
- **检查：** 图片是否清晰，人脸是否正面

### 2. 签到任务时间验证失败
- **原因：** 结束时间必须晚于当前时间
- **解决：** 使用 `(Get-Date).AddHours(2)` 生成未来时间

### 3. 权限验证失败
- **检查：** Session/Cookie是否正确传递
- **检查：** 用户角色是否正确

### 4. 数据库连接失败
- **检查：** `app.js` 中的 `dbConfig` 配置
- **检查：** MySQL服务是否启动
- **检查：** 数据库表结构是否已初始化

### 5. Socket.IO 连接失败
- **检查：** 服务器是否正常启动（应看到 `Socket.IO 已启用`）
- **检查：** 是否已登录（Socket 需要 Session Cookie）
- **检查：** 端口 3000 是否被占用
- **检查：** 防火墙是否阻止 WebSocket 连接

### 6. Socket.IO 收不到推送
- **检查：** 学生是否已连接 Socket.IO
- **检查：** 学生是否在正确的班级房间（服务器日志显示 `加入房间: class-X`）
- **检查：** 任务的 classId 是否与学生的 classId 匹配
- **检查：** 服务器日志是否显示推送消息：`[Socket.IO] 签到任务已推送给房间: class-X`

---

## 📝 测试数据清理建议

测试完成后，可以清理测试数据：

```sql
-- 删除测试学生的签到记录
DELETE FROM attendance_record WHERE userId = 10;

-- 删除测试签到任务
DELETE FROM attendance_task WHERE taskName LIKE '%测试%';

-- 删除测试学生账号
DELETE FROM user WHERE userAccount = 'student001';

-- 清理训练数据文件（在文件系统中）
-- 删除 opencv/face_get/Facedata/ 目录中对应的图片文件
```

---

## 🎯 快速测试命令总结

### 学生完整流程（PowerShell）
```powershell
# 1. 获取班级列表
Invoke-WebRequest -Uri "http://localhost:3000/api/classes" | Select-Object -ExpandProperty Content

# 2. 学生注册
$body = @{userAccount="student001";userPassword="Test123";userName="测试";classId=1} | ConvertTo-Json
Invoke-WebRequest -Uri "http://localhost:3000/api/register" -Method POST -Body $body -ContentType "application/json" | Select-Object -ExpandProperty Content

# 3. 学生登录
$login = @{userAccount="student001";userPassword="Test123"} | ConvertTo-Json
Invoke-WebRequest -Uri "http://localhost:3000/api/login" -Method POST -Body $login -ContentType "application/json" -SessionVariable sess | Select-Object -ExpandProperty Content

# 4. 人脸注册
$form = @{imagefile=Get-Item "test.jpg"; userId="10"}
Invoke-WebRequest -Uri "http://localhost:3000/api/face-register" -Method POST -Form $form | Select-Object -ExpandProperty Content

# 5. 查看任务（REST API 方式）
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-tasks" -WebSession $sess | Select-Object -ExpandProperty Content

# 5.1 连接 Socket.IO 接收实时推送（推荐）
# 使用测试脚本：node test_socket_cli.js
# 或在前端页面连接 Socket.IO

# 6. 人脸识别签到
$form = @{imagefile=Get-Item "test.jpg"; taskId="1"}
Invoke-WebRequest -Uri "http://localhost:3000/api/face-recognition" -Method POST -Form $form | Select-Object -ExpandProperty Content
```

### 教师完整流程（PowerShell）
```powershell
# 1. 教师登录
$login = @{userAccount="teacher001";userPassword="123456"} | ConvertTo-Json
Invoke-WebRequest -Uri "http://localhost:3000/api/login" -Method POST -Body $login -ContentType "application/json" -SessionVariable tsess | Select-Object -ExpandProperty Content

# 2. 发布任务（会自动通过 Socket.IO 推送）
$start = (Get-Date).AddMinutes(5).ToString("yyyy-MM-dd HH:mm:ss")
$end = (Get-Date).AddHours(2).ToString("yyyy-MM-dd HH:mm:ss")
$task = @{taskName="测试任务";classId=1;startTime=$start;endTime=$end} | ConvertTo-Json
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-task" -Method POST -Body $task -ContentType "application/json" -WebSession $tsess | Select-Object -ExpandProperty Content
# 注意：发布成功后，如果目标班级有学生在线，会立即收到 Socket.IO 推送

# 3. 查看任务列表
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-tasks" -WebSession $tsess | Select-Object -ExpandProperty Content

# 4. 查看签到统计
Invoke-WebRequest -Uri "http://localhost:3000/api/attendance-stats?taskId=1" -WebSession $tsess | Select-Object -ExpandProperty Content
```

---

**测试完成！** 请按照以上步骤逐项测试，确保所有功能正常工作。如有问题，请参考错误信息和日志进行排查。

