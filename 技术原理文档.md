# 人脸识别签到系统技术原理文档

## 目录

1. [系统架构概述](#系统架构概述)
2. [技术栈详解](#技术栈详解)
3. [数据库设计原理](#数据库设计原理)
4. [认证与授权机制](#认证与授权机制)
5. [人脸识别技术原理](#人脸识别技术原理)
6. [课程管理系统原理](#课程管理系统原理)
7. [签到系统原理](#签到系统原理)
8. [前后端交互机制](#前后端交互机制)
9. [安全机制](#安全机制)
10. [性能优化策略](#性能优化策略)

---

## 系统架构概述

### 整体架构设计

```
┌─────────────────┐
│   前端 (UniApp)  │  Vue.js + uni-app 跨平台框架
│   - 用户界面     │  - 支持 H5、小程序、App
│   - 业务逻辑     │  - 响应式设计
│   - 状态管理     │
└────────┬────────┘
         │ HTTP/HTTPS + JWT
         │ RESTful API
         ▼
┌─────────────────┐
│ 后端 (Node.js)   │  Express.js 框架
│   - API 服务     │  - RESTful 接口设计
│   - 业务逻辑     │  - JWT 认证授权
│   - 数据验证     │  - 文件上传处理
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌──────────┐
│ MySQL  │ │ Python   │
│ 数据库 │ │ 算法服务 │
└────────┘ └──────────┘
```

### 架构特点

1. **前后端分离**
   - 前端专注 UI 展示和用户交互
   - 后端专注业务逻辑和数据处理
   - 通过 RESTful API 进行通信

2. **微服务思想**
   - 算法服务（Python）独立部署
   - 后端服务（Node.js）独立部署
   - 数据库服务（MySQL）独立部署
   - 各服务可独立升级和维护

3. **数据驱动**
   - 以数据库为中心的数据流转
   - 关系型数据库保证数据一致性
   - 外键约束保证数据完整性

4. **无状态设计**
   - 使用 JWT 进行无状态认证
   - 支持水平扩展
   - 便于负载均衡

---

## 技术栈详解

### 前端技术栈

#### 1. UniApp 框架
- **技术**: Vue.js + uni-app
- **优势**:
  - 一套代码多端运行（H5、小程序、App）
  - 丰富的组件库和 API
  - 良好的性能表现
- **核心特性**:
  - 响应式数据绑定
  - 组件化开发
  - 路由管理
  - 状态管理

#### 2. HTTP 请求
- **技术**: `uni.request`
- **功能**:
  - 封装统一的请求方法
  - 自动添加 JWT Token
  - 统一错误处理
  - 请求拦截和响应拦截

#### 3. 本地存储
- **技术**: `uni.setStorageSync` / `uni.getStorageSync`
- **用途**:
  - 存储 JWT Token
  - 存储用户信息
  - 存储网络配置

### 后端技术栈

#### 1. Node.js + Express.js
- **技术**: Node.js 运行时 + Express.js 框架
- **优势**:
  - JavaScript 全栈开发
  - 异步非阻塞 I/O
  - 丰富的生态系统
  - 快速开发迭代

#### 2. MySQL 数据库
- **技术**: MySQL 5.7+
- **特点**:
  - 关系型数据库
  - ACID 特性保证数据一致性
  - 支持事务处理
  - 连接池管理

#### 3. JWT 认证
- **技术**: `jsonwebtoken`
- **功能**:
  - 无状态认证
  - Token 签名和验证
  - 用户信息编码

#### 4. 文件上传
- **技术**: `multer`
- **功能**:
  - 处理 `multipart/form-data`
  - 文件大小限制
  - 文件重命名
  - 存储管理

#### 5. 密码加密
- **技术**: `bcrypt`
- **特点**:
  - 单向哈希加密
  - 盐值随机生成
  - 抗彩虹表攻击

### 算法服务技术栈

#### 1. Python + OpenCV
- **技术**: Python 3.x + OpenCV
- **功能**:
  - 人脸检测（Haar Cascade）
  - 人脸识别（LBPH 算法）
  - 模型训练

#### 2. LBPH 算法
- **全称**: Local Binary Patterns Histograms
- **原理**:
  - 提取人脸局部二值模式特征
  - 构建直方图特征向量
  - 计算相似度进行匹配
- **优势**:
  - 对光照变化不敏感
  - 计算效率高
  - 适合实时识别

---

## 数据库设计原理

### 数据库架构

```
class (班级表)
  │
  ├── user (用户表) ──┐
  │                    │
  │                    ├── course (课程表)
  │                    │     │
  │                    │     ├── course_class (课程-班级关联表)
  │                    │     │
  │                    │     ├── student_course (学生-课程关联表)
  │                    │     │
  │                    │     └── attendance_task (签到任务表)
  │                    │
  │                    └── attendance_record (签到记录表)
```

### 核心表设计

#### 1. 用户表 (user)

```sql
CREATE TABLE user (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  userAccount VARCHAR(50) NOT NULL UNIQUE,
  userPassword VARCHAR(100) NOT NULL,
  userName VARCHAR(50) NULL,
  userRole ENUM('student', 'teacher') NOT NULL DEFAULT 'student',
  classId BIGINT NULL,
  faceRegistered TINYINT DEFAULT 0,
  createTime DATETIME DEFAULT CURRENT_TIMESTAMP,
  updateTime DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (classId) REFERENCES class(id) ON DELETE SET NULL
);
```

**设计原理**:
- **主键自增**: 保证唯一性，提高插入性能
- **唯一约束**: `userAccount` 唯一，防止重复注册
- **角色枚举**: 限制角色类型，保证数据一致性
- **外键约束**: `classId` 关联班级表，保证数据完整性
- **时间戳**: 自动记录创建和更新时间
- **状态字段**: `faceRegistered` 跟踪人脸注册状态

#### 2. 课程表 (course)

```sql
CREATE TABLE course (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  courseName VARCHAR(100) NOT NULL,
  courseCode VARCHAR(50) NOT NULL UNIQUE,
  teacherId BIGINT NOT NULL,
  description TEXT NULL,
  FOREIGN KEY (teacherId) REFERENCES user(id) ON DELETE CASCADE
);
```

**设计原理**:
- **课程代码唯一**: `courseCode` 唯一标识课程
- **外键级联删除**: 删除老师时自动删除其课程
- **描述字段**: 支持课程详细说明

#### 3. 课程-班级关联表 (course_class)

```sql
CREATE TABLE course_class (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  courseId BIGINT NOT NULL,
  classId BIGINT NOT NULL,
  UNIQUE KEY uk_course_class (courseId, classId),
  FOREIGN KEY (courseId) REFERENCES course(id) ON DELETE CASCADE,
  FOREIGN KEY (classId) REFERENCES class(id) ON DELETE CASCADE
);
```

**设计原理**:
- **多对多关系**: 一个课程可以关联多个班级，一个班级可以关联多个课程
- **唯一约束**: 防止重复关联
- **级联删除**: 删除课程或班级时自动删除关联

#### 4. 学生-课程关联表 (student_course)

```sql
CREATE TABLE student_course (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  studentId BIGINT NOT NULL,
  courseId BIGINT NOT NULL,
  createTime DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_student_course (studentId, courseId),
  FOREIGN KEY (studentId) REFERENCES user(id) ON DELETE CASCADE,
  FOREIGN KEY (courseId) REFERENCES course(id) ON DELETE CASCADE
);
```

**设计原理**:
- **选课关系**: 记录学生选课信息
- **唯一约束**: 防止重复选课
- **时间记录**: 记录选课时间

#### 5. 签到任务表 (attendance_task)

```sql
CREATE TABLE attendance_task (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  taskName VARCHAR(100) NOT NULL,
  teacherId BIGINT NOT NULL,
  courseId BIGINT NOT NULL,
  startTime DATETIME NOT NULL,
  endTime DATETIME NOT NULL,
  status ENUM('active', 'inactive', 'completed') DEFAULT 'active',
  FOREIGN KEY (teacherId) REFERENCES user(id) ON DELETE CASCADE,
  FOREIGN KEY (courseId) REFERENCES course(id) ON DELETE CASCADE,
  INDEX idx_time (startTime, endTime)
);
```

**设计原理**:
- **课程关联**: 签到任务关联到具体课程
- **时间范围**: 记录签到开始和结束时间
- **状态管理**: 跟踪任务状态
- **时间索引**: 优化时间范围查询

#### 6. 签到记录表 (attendance_record)

```sql
CREATE TABLE attendance_record (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  userId BIGINT NOT NULL,
  taskId BIGINT NULL,
  checkTime DATETIME DEFAULT CURRENT_TIMESTAMP,
  status TINYINT DEFAULT 1,
  FOREIGN KEY (userId) REFERENCES user(id) ON DELETE CASCADE,
  FOREIGN KEY (taskId) REFERENCES attendance_task(id) ON DELETE SET NULL,
  INDEX idx_user_time (userId, checkTime),
  INDEX idx_task (taskId)
);
```

**设计原理**:
- **用户关联**: 记录签到用户
- **任务关联**: 可选关联签到任务
- **时间索引**: 优化用户签到历史查询
- **状态记录**: 记录签到成功/失败状态

### 索引设计原理

1. **主键索引**: 所有表都有自增主键，保证唯一性和查询性能
2. **唯一索引**: 防止重复数据（账号、课程代码等）
3. **外键索引**: 优化关联查询性能
4. **联合索引**: `idx_user_time` 优化用户签到历史查询
5. **时间索引**: `idx_time` 优化时间范围查询

---

## 认证与授权机制

### JWT 认证原理

#### 1. JWT 结构

JWT (JSON Web Token) 由三部分组成：
```
Header.Payload.Signature
```

- **Header**: 算法类型和 Token 类型
- **Payload**: 用户信息（ID、角色、班级等）
- **Signature**: 使用密钥签名的哈希值

#### 2. Token 生成

```javascript
function signAccessToken(user) {
    const payload = {
        id: user.id,
        userAccount: user.userAccount,
        userName: user.userName,
        userRole: user.userRole,
        classId: user.classId,
        className: user.className || null,
        faceRegistered: user.faceRegistered
    };
    return jwt.sign(payload, JWT_SECRET, { expiresIn: '2h' });
}
```

**原理**:
- 将用户信息编码到 Payload
- 使用密钥签名防止篡改
- 设置过期时间（默认 2 小时）

#### 3. Token 验证

```javascript
const requireLogin = (req, res, next) => {
    const token = extractTokenFromRequest(req);
    if (!token) {
        return res.status(401).json({ success: false, message: '未携带访问令牌' });
    }
    try {
        const decoded = jwt.verify(token, JWT_SECRET);
        req.user = decoded;
        next();
    } catch (e) {
        return res.status(401).json({ success: false, message: '访问令牌无效或已过期' });
    }
};
```

**原理**:
- 从请求头提取 Token
- 使用密钥验证签名
- 解析 Payload 获取用户信息
- 将用户信息附加到请求对象

#### 4. Token 提取

```javascript
function extractTokenFromRequest(req) {
    // 从 Authorization 头提取
    const header = req.headers.authorization || req.headers.Authorization;
    if (header && header.startsWith('Bearer ')) {
        return header.substring(7);
    }
    // 从自定义头提取
    if (req.headers['x-access-token']) {
        return req.headers['x-access-token'];
    }
    // 从 Cookie 提取
    if (req.cookies && req.cookies.access_token) {
        return req.cookies.access_token;
    }
    return null;
}
```

**原理**:
- 支持多种 Token 传递方式
- 优先从 Authorization 头提取
- 兼容自定义头和 Cookie

### 登录流程

```
用户输入账号密码
    ↓
前端验证格式
    ↓
POST /api/login
    ↓
后端查询用户
    ↓
bcrypt 验证密码
    ↓
生成 JWT Token
    ↓
返回 Token + 用户信息
    ↓
前端存储 Token
```

### 权限控制

#### 1. 角色定义
- **student**: 学生角色
- **teacher**: 老师角色

#### 2. 权限矩阵

| 功能 | 学生 | 老师 |
|------|------|------|
| 注册账号 | ✅ | ❌ |
| 登录系统 | ✅ | ✅ |
| 人脸注册 | ✅ | ✅ |
| 人脸识别签到 | ✅ | ❌ |
| 创建课程 | ❌ | ✅ |
| 管理课程 | ❌ | ✅ |
| 选课/退课 | ✅ | ❌ |
| 发布签到任务 | ❌ | ✅ |
| 查看签到任务 | ✅ (仅已选课程) | ✅ (自己发布的) |
| 查看签到统计 | ❌ | ✅ |

#### 3. 权限验证

```javascript
// 角色验证中间件
const requireRole = (roles) => {
    return (req, res, next) => {
        if (!req.user) {
            return res.status(401).json({ success: false, message: '未登录' });
        }
        if (!roles.includes(req.user.userRole)) {
            return res.status(403).json({ success: false, message: '权限不足' });
        }
        next();
    };
};

// 使用示例
app.post('/api/courses', requireLogin, requireRole(['teacher']), createCourse);
```

---

## 人脸识别技术原理

### 整体流程

```
上传图片
    ↓
保存到 public 目录
    ↓
调用 Python 识别脚本
    ↓
OpenCV 人脸检测
    ↓
LBPH 特征提取
    ↓
模型匹配
    ↓
返回识别结果
```

### 人脸检测

#### 1. Haar Cascade 分类器

```python
face_cascade = cv2.CascadeClassifier('haarcascade_frontalface_default.xml')
faces = face_cascade.detectMultiScale(gray, 1.2, 5)
```

**原理**:
- 使用 Haar 特征描述人脸
- 级联分类器快速检测
- 多尺度检测适应不同大小

#### 2. 图像预处理

```python
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
```

**原理**:
- 转换为灰度图减少计算量
- 统一图像格式

### 人脸识别

#### 1. LBPH 算法原理

**LBP (Local Binary Pattern)**:
- 将图像分成多个小区域
- 对每个区域计算局部二值模式
- 构建直方图特征向量

**优势**:
- 对光照变化不敏感
- 计算效率高
- 适合实时识别

#### 2. 模型训练

```python
recognizer = cv2.face.LBPHFaceRecognizer_create()
recognizer.train(faces, labels)
recognizer.save('trainer.yml')
```

**流程**:
1. 收集人脸样本（Facedata 目录）
2. 提取 LBPH 特征
3. 训练分类器
4. 保存模型

#### 3. 人脸识别

```python
idnum, conf = recognizer.predict(face)
if conf < 200:  # 置信度阈值
    # 识别成功
    result = {
        "recognized": True,
        "userId": int(idnum),
        "userName": name
    }
```

**原理**:
- `predict` 返回用户 ID 和置信度
- 置信度越小表示越相似
- 阈值 200 用于判断是否识别成功

### 人脸注册流程

```
用户上传人脸照片
    ↓
保存到 public 目录
    ↓
调用 Python 识别脚本验证
    ↓
验证是否为该用户本人
    ↓
复制到 Facedata 目录
    ↓
重新训练模型
    ↓
更新用户状态
```

### 冷启动机制

**首次注册**:
- `faceRegistered = 0` 时跳过识别验证
- 直接保存样本并训练
- 避免首次注册无法识别的问题

---

## 课程管理系统原理

### 课程创建流程

```
老师登录
    ↓
创建课程表单
    ↓
POST /api/courses
    ↓
验证课程代码唯一性
    ↓
保存到数据库
    ↓
返回课程信息
```

### 课程-班级关联

#### 1. 关联原理

- **多对多关系**: 一个课程可以关联多个班级，一个班级可以关联多个课程
- **关联表**: `course_class` 存储关联关系

#### 2. 关联流程

```
老师选择课程
    ↓
选择要关联的班级
    ↓
POST /api/courses/:id/classes
    ↓
批量插入关联记录
    ↓
返回关联结果
```

### 学生选课机制

#### 1. 选课原理

- **显式选课**: 学生必须主动选择课程
- **选课表**: `student_course` 记录选课关系
- **唯一约束**: 防止重复选课

#### 2. 选课流程

```
学生浏览课程列表
    ↓
查看课程详情
    ↓
POST /api/courses/:id/select
    ↓
检查是否已选课
    ↓
插入选课记录
    ↓
返回选课结果
```

#### 3. 课程可见性

**学生视角**:
- 显示所有课程
- 标记已选课程 (`isSelected`)
- 标记班级关联课程 (`isClassRelated`)

**老师视角**:
- 只显示自己创建的课程

---

## 签到系统原理

### 签到任务发布

#### 1. 发布流程

```
老师选择课程
    ↓
输入任务名称和时长
    ↓
POST /api/attendance-task
    ↓
验证课程归属
    ↓
计算开始和结束时间
    ↓
保存到数据库
    ↓
(可选) Socket.IO 推送通知
```

#### 2. 时间计算

```javascript
const startTime = new Date();
const endTime = new Date(startTime.getTime() + duration * 60 * 1000);
```

**原理**:
- 开始时间: 当前时间
- 结束时间: 开始时间 + 时长（分钟）

### 签到识别流程

#### 1. 完整流程

```
学生选择签到任务
    ↓
上传人脸照片
    ↓
POST /api/face-recognition
    ↓
保存图片到 public 目录
    ↓
调用 Python 识别脚本
    ↓
验证任务有效性
    ↓
验证学生是否选课
    ↓
验证时间范围
    ↓
检查是否已签到
    ↓
识别成功 → 记录签到
    ↓
返回签到结果
```

#### 2. 任务验证

```javascript
// 验证任务是否存在
const [taskRows] = await connection.execute(
    'SELECT * FROM attendance_task WHERE id = ?',
    [taskId]
);

// 验证学生是否选课
const [courseRows] = await connection.execute(
    `SELECT sc.courseId FROM student_course sc
     INNER JOIN attendance_task at ON sc.courseId = at.courseId
     WHERE sc.studentId = ? AND at.id = ?`,
    [currentUserId, taskId]
);

// 验证时间范围
const now = new Date();
if (now < task.startTime || now > task.endTime) {
    return res.status(400).json({ success: false, message: '不在签到时间内' });
}
```

#### 3. 重复签到检查

```javascript
const [existingRecords] = await connection.execute(
    'SELECT * FROM attendance_record WHERE userId = ? AND taskId = ?',
    [userId, taskId]
);
if (existingRecords.length > 0) {
    return res.status(400).json({ success: false, message: '已经签到过了' });
}
```

### 签到统计原理

#### 1. 统计范围

**只统计已选课学生**:
```sql
SELECT COUNT(DISTINCT sc.studentId) as totalStudents
FROM student_course sc
WHERE sc.courseId = ?
```

**统计已签到学生**:
```sql
SELECT COUNT(DISTINCT ar.userId) as checkedInStudents
FROM attendance_record ar
INNER JOIN student_course sc ON ar.userId = sc.studentId
WHERE ar.taskId = ? AND sc.courseId = ?
```

#### 2. 详细统计

```sql
SELECT 
    u.id, u.userAccount, u.userName,
    CASE WHEN ar.id IS NOT NULL THEN 1 ELSE 0 END as hasCheckedIn,
    ar.checkTime
FROM student_course sc
INNER JOIN user u ON sc.studentId = u.id
LEFT JOIN attendance_record ar ON ar.userId = u.id AND ar.taskId = ?
WHERE sc.courseId = ?
ORDER BY u.userAccount
```

**原理**:
- 左连接获取所有选课学生
- 左连接签到记录判断是否已签到
- 按学生账号排序

### 人脸识别优先级

**问题**: 多个用户使用同一张照片

**解决方案**:
```javascript
if (currentUserRole === 'student') {
    // 学生：始终使用当前登录的用户ID
    targetUserId = currentUserId;
} else {
    // 老师：使用识别出的用户ID（用于管理场景）
    targetUserId = recognitionResult.userId;
}
```

**原理**:
- 学生签到以当前登录用户为准
- 避免因照片重复导致识别错误
- 保证签到记录的准确性

---

## 前后端交互机制

### HTTP 请求封装

#### 1. 请求拦截

```javascript
// 自动添加 Token
const token = uni.getStorageSync('accessToken');
if (token) {
    header['Authorization'] = `Bearer ${token}`;
}
```

#### 2. 响应拦截

```javascript
// 统一错误处理
if (res.statusCode === 401) {
    // Token 过期，跳转登录
    uni.removeStorageSync('accessToken');
    uni.reLaunch({ url: '/pages/login/login' });
}
```

### 数据格式

#### 1. 请求格式

```javascript
// POST 请求
uni.request({
    url: 'http://localhost:3000/api/login',
    method: 'POST',
    header: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer <token>'
    },
    data: {
        userAccount: 'student001',
        userPassword: '123456'
    }
});
```

#### 2. 响应格式

```javascript
{
    success: true,        // 操作是否成功
    message: "登录成功",   // 用户友好的消息
    data: {               // 实际数据
        accessToken: "...",
        user: { ... }
    }
}
```

### 文件上传

#### 1. 上传格式

```javascript
uni.uploadFile({
    url: 'http://localhost:3000/api/face-recognition',
    filePath: tempFilePath,
    name: 'imagefile',
    formData: {
        taskId: '123'
    },
    header: {
        'Authorization': 'Bearer <token>'
    }
});
```

#### 2. 后端处理

```javascript
const upload = multer({
    storage: multer.diskStorage({
        destination: './public',
        filename: (req, file, cb) => {
            const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
            cb(null, 'imagefile-' + uniqueSuffix + path.extname(file.originalname));
        }
    }),
    limits: { fileSize: 20 * 1024 * 1024 }  // 20MB
});
```

---

## 安全机制

### 1. 密码安全

#### bcrypt 加密

```javascript
// 注册时加密
const hashedPassword = await bcrypt.hash(password, 10);

// 登录时验证
const isValid = await bcrypt.compare(password, hashedPassword);
```

**原理**:
- **盐值随机**: 每次加密使用不同盐值
- **不可逆**: 无法从哈希值反推原密码
- **抗彩虹表**: 盐值机制防止预计算攻击
- **成本可调**: rounds 参数控制计算复杂度（默认 10）

### 2. SQL 注入防护

#### 预处理语句

```javascript
// ❌ 不安全
const query = `SELECT * FROM user WHERE userAccount = '${userAccount}'`;

// ✅ 安全
const [rows] = await connection.execute(
    'SELECT * FROM user WHERE userAccount = ?',
    [userAccount]
);
```

**原理**:
- 参数化查询防止 SQL 注入
- 数据库自动转义特殊字符
- 类型检查防止类型混淆

### 3. XSS 防护

#### 输入验证

```javascript
// 账号格式验证
const accountRegex = /^[a-zA-Z0-9_]{3,20}$/;
if (!accountRegex.test(userAccount)) {
    return res.status(400).json({ success: false, message: '账号格式不正确' });
}
```

**原理**:
- 限制输入字符集
- 限制输入长度
- 过滤特殊字符

### 4. 文件上传安全

#### 文件类型限制

```javascript
const allowedTypes = ['image/jpeg', 'image/png', 'image/webp'];
if (!allowedTypes.includes(file.mimetype)) {
    return res.status(400).json({ success: false, message: '不支持的文件类型' });
}
```

#### 文件大小限制

```javascript
limits: { fileSize: 20 * 1024 * 1024 }  // 20MB
```

#### 文件名安全

```javascript
const uniqueSuffix = Date.now() + '-' + Math.round(Math.random() * 1E9);
const filename = 'imagefile-' + uniqueSuffix + path.extname(file.originalname);
```

**原理**:
- 使用时间戳和随机数生成唯一文件名
- 防止文件名冲突
- 防止路径遍历攻击

### 5. CORS 配置

```javascript
app.use((req, res, next) => {
    res.header('Access-Control-Allow-Origin', '*');
    res.header('Access-Control-Allow-Headers', '*');
    res.header('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');
    if (req.method === 'OPTIONS') {
        return res.sendStatus(200);
    }
    next();
});
```

**注意**: 生产环境应限制 `Access-Control-Allow-Origin` 为具体域名

### 6. JWT 安全

#### 密钥管理

```javascript
const JWT_SECRET = process.env.JWT_SECRET || 'face-recognition-jwt-secret-CHANGE_ME';
```

**建议**:
- 使用环境变量存储密钥
- 生产环境使用强随机密钥
- 定期轮换密钥

#### Token 过期

```javascript
{ expiresIn: '2h' }  // 2 小时过期
```

**原理**:
- 限制 Token 有效期
- 过期后需重新登录
- 减少 Token 泄露风险

---

## 性能优化策略

### 1. 数据库优化

#### 连接池管理

```javascript
const pool = mysql.createPool({
    connectionLimit: 10,      // 最大连接数
    acquireTimeout: 60000,    // 获取连接超时
    timeout: 60000            // 查询超时
});
```

**原理**:
- 复用数据库连接
- 减少连接创建开销
- 控制并发连接数

#### 索引优化

```sql
-- 联合索引优化查询
INDEX idx_user_time (userId, checkTime)

-- 时间范围查询索引
INDEX idx_time (startTime, endTime)
```

**原理**:
- 加速常用查询
- 减少全表扫描
- 提高查询性能

#### 查询优化

```sql
-- 使用 LIMIT 分页
SELECT * FROM attendance_record 
WHERE userId = ? 
ORDER BY checkTime DESC 
LIMIT 10 OFFSET 0;

-- 只查询需要的字段
SELECT id, userName, userAccount FROM user WHERE id = ?;
```

### 2. 文件处理优化

#### 异步处理

```javascript
// 异步调用 Python 脚本
const py = spawn('python', [scriptPath, imagePath]);
```

**原理**:
- 不阻塞主线程
- 提高并发处理能力
- 改善用户体验

#### 文件大小限制

```javascript
limits: { fileSize: 20 * 1024 * 1024 }  // 20MB
```

**原理**:
- 防止大文件占用过多资源
- 保护服务器存储空间
- 提高处理速度

### 3. 缓存策略

#### 课程列表缓存

```javascript
// 前端缓存课程列表
const courses = uni.getStorageSync('courses');
if (courses && Date.now() - courses.timestamp < 5 * 60 * 1000) {
    // 使用缓存数据（5 分钟内有效）
    return courses.data;
}
```

**原理**:
- 减少网络请求
- 提高响应速度
- 改善用户体验

### 4. 代码优化

#### 错误处理

```javascript
try {
    const result = await someOperation();
    res.json({ success: true, data: result });
} catch (error) {
    console.error('操作失败:', error);
    res.status(500).json({ success: false, message: '服务器内部错误' });
}
```

**原理**:
- 统一错误处理
- 记录错误日志
- 返回友好错误信息

#### 资源释放

```javascript
const connection = await pool.getConnection();
try {
    // 使用连接
    const [rows] = await connection.execute('SELECT * FROM user');
} finally {
    connection.release();  // 确保释放连接
}
```

**原理**:
- 及时释放数据库连接
- 防止连接泄漏
- 提高资源利用率

---

## 总结

本系统采用前后端分离架构，使用 JWT 进行无状态认证，通过 MySQL 存储数据，使用 Python + OpenCV 实现人脸识别功能。系统设计遵循 RESTful 原则，注重安全性和性能优化，支持课程管理、学生选课、签到任务发布和统计等功能。

### 核心技术点

1. **JWT 无状态认证**: 支持水平扩展，便于负载均衡
2. **LBPH 人脸识别**: 对光照不敏感，适合实时识别
3. **关系型数据库设计**: 保证数据一致性和完整性
4. **前后端分离**: 便于独立开发和部署
5. **安全机制**: 密码加密、SQL 注入防护、文件上传安全

### 扩展性

- 支持多端运行（H5、小程序、App）
- 算法服务可独立升级
- 数据库可水平扩展
- API 接口标准化，便于集成

---

**文档版本**: v1.0  
**最后更新**: 2024年  
**维护者**: 开发团队

